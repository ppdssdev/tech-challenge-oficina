# Credenciais exclusivas para execução local/demonstração.
# Em produção, use um gerenciador de secrets apropriado e não versione valores reais.
apiVersion: v1
kind: Secret
metadata:
  name: oficina-api-secret
  namespace: oficina
type: Opaque
stringData:
  SPRING_DATASOURCE_PASSWORD: oficina
  APP_ADMIN_DEFAULT_PASSWORD: admin123
  APP_JWT_SECRET: awssecretpostech-oficina-fiap-2026
